import { ContentContainer } from "../../components/ContentContainer";

export function AddClient() {
    return (
        <ContentContainer>
            <h1>Add Client</h1>
            {/* Form or additional content for adding a client can be added here */}
        </ContentContainer>
    );
}