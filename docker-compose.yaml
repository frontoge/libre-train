services:
    server:
        build:
            context: .
            dockerfile: server/Dockerfile
        restart: unless-stopped
        ports:
            - '3000:3000'
        env_file:
            - ./server/.env
